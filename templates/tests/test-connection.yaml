apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "my-argocd.fullname" . }}-test-connection"
  labels:
    {{- include "my-argocd.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "my-argocd.fullname" . }}:{{ .Values.app1.service.port }}']
  restartPolicy: Never
